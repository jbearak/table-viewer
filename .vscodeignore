src/**
node_modules/**
out/**
tsconfig.json
tsconfig.integration.json
.vscode-test.mjs
desktop/**
dist/desktop/**
dist/desktop-packages/**
# electron-builder's legacy/default unpacked output layouts. These app bundles
# contain symlinks to directories, which vsce's secret scan cannot read as files.
dist/mac*/**
dist/*-unpacked/**
dist/runtime-probes/**
.node-version
.gitignore
.superpowers/**
docs/**
.claude/**
.github/**
scripts/**
vitest.config.ts
test-results/**
# Desktop icon master (electron-builder buildResources); media/icon-128.png is
# the one the marketplace listing uses and must stay in the .vsix.
build/**
design_handoff_app_icon/**
